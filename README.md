# CareerKit Mobile Dashboard

A simple Flutter app that checks whether a CV has the basic sections needed for an internship, junior developer role, or general job application.

The app uses:

- Stateful input handling
- Text fields
- A dropdown menu
- Checkboxes
- A score calculation
- A result summary
